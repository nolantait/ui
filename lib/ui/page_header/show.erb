<article class="ui-page-header">
  <%= breadcrumbs %>

  <header>
    <%= back_button %>

    <hgroup>
      <%= title %>
      <%= subheading %>
    </hgroup>

    <nav>
      <%= actions %>
    </nav>
  </header>

  <%= yield if block_given? %>
</article>
